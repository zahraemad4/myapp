<%= form_with(model: job) do |form| %>
  <% if job.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id, User.all.pluck(:name,:id)%>
  </div>

  <div>
    <%= form.label :jobtitle, style: "display: block" %>
    <%= form.text_field :jobtitle %>
  </div>

  <div>
    <%= form.label :workhours, style: "display: block" %>
    <%= form.number_field :name, {min: 0, max: 24} %>
  </div>

  <div>
    <%= form.label :company, style: "display: block" %>
    <%= form.text_field :company %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
